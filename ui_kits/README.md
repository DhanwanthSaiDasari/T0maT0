# Tomato — UI Kits

Two surfaces:

- `app/` — the timer itself. Single screen, keyboard-driven, lives in a window or browser tab.
- `marketing/` — the one-page site that links to it.

Each is a working click-thru recreation, not production code.
